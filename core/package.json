{
  "name": "@easytocloud/claude-personae-core",
  "version": "0.1.0",
  "private": true,
  "description": "Shared preset-resolution engine for Claude Code Personae (VS Code extension + CLI). Bundled into both consumers — not published.",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "watch": "tsc -b --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.6.0"
  }
}
